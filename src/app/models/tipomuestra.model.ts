import { SilacConfig } from "./utils/silacConfig.model";

export class TipoMuestra extends SilacConfig {
    idTipoMuestra?: string;
    codigoTipoMuestra?: string;
    codigo?: string;
    nombre?: string;
    descripcion?: string;
}