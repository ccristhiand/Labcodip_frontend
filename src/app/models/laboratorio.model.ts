import { SilacConfig } from "./utils/silacConfig.model";

export class Laboratorio extends SilacConfig {
    idLaboratorio?: string;
    codigoLaboratorio?: string;
    codigo?: string;
    nombre?: string;
}